"""Migration importers. The agent importers (migrate.py) write agent definitions
from other frameworks into a workspace's agents/ and skills/; the wallet and
on-chain importers write what they read as JSON files under imported/<platform>/
and nothing into the gateway's database. See migration/README.md."""
