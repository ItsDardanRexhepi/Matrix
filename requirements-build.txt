# The Matrix — the image builder's own tooling (the lock)
#
# Installed into /opt/venv by the Dockerfile's builder stage, before
# requirements.txt, with --require-hashes. It replaced an unpinned
# `pip install --upgrade pip setuptools wheel`, which put whatever pip and
# setuptools were newest that day into every image. The versions are the
# reference environment's (the one the suite runs on).
#
# `wheel` is gone rather than pinned: the builder installs requirements.txt
# with --only-binary=:all:, so nothing is built, and pip installs wheels
# without the `wheel` package. setuptools stays because `python -m venv` puts
# one in the venv the runtime image copies; this makes it a known version
# instead of the base image's bundled one.
#
# To bump: edit a version, then run scripts/relock_requirements.sh.
#
# ── GENERATED by scripts/relock_requirements.sh — pins and hashes below ──────
pip==26.2.1 \
    --hash=sha256:71138adf1f4ca900cdb7d289c21b7494329f2332b6d85f0e1c42108c0384ed3e \
    --hash=sha256:f6ad667e89a1fe78046c8f13232b247200f5258d7828f3f7883d660878e0813f
setuptools==82.0.0 \
    --hash=sha256:22e0a2d69474c6ae4feb01951cb69d515ed23728cf96d05513d36e42b62b37cb \
    --hash=sha256:70b18734b607bd1da571d097d236cfcfacaf01de45717d59e6e04b96877532e0
