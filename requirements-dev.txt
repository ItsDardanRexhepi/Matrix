# Development and CI-only dependencies.
#
# Install with TWO commands, runtime lock first:
#   pip install -r requirements.txt && pip install -r requirements-dev.txt
#
# requirements.txt carries hashes, and pip applies hash checking to every
# requirement in a command once any one has a hash. These entries have none,
# so this file no longer includes requirements.txt (`-r`), and the two cannot
# be given to one pip command. tests/test_dependency_manifests.py fails if a
# tool installed here moves a locked runtime package.
#
# None of these are baked into the production Docker image.

# Test runner + fixtures
pytest~=8.3
pytest-asyncio~=0.24
pytest-aiohttp~=1.0
pytest-cov~=5.0

# Type and docstring checks
mypy~=1.11
interrogate~=1.7

# Docs
pdoc~=15.0

# Security
pip-audit~=2.7
# PINNED, not ~=. bandit gates the build (NEW-2 removed its `|| true`), so the
# version IS part of what a green security job claims. `~=1.7` accepted 1.9.4,
# whose rule set differs from the one the gate was written against — the same
# shape as the SwiftLint drift that failed a tree-identical commit next door
# (Matrix register entry::UNREPRODUCIBLE-GREEN). Bump deliberately and fix what
# the new version finds in the same change.
bandit==1.9.4

# Lint
flake8~=7.1
